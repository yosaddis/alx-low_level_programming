#include "main.h"
#include <stdio.h>

int main(void)
{
	char *str;
	int len;

	str = "My first strlen!";
	//str = "";
	//	len = _strlen(str);
//	_puts(str);
	//printf("%s\n", str);
	//rev_string(str);
	//printf("%s\n",str);
	puts_half(str);
	return (0);
}
