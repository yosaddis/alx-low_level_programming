#include "main.h"
#include <stdio.h>

int main(void)
{
	char *str;
	int len;

	str = "My first strlen!";
	//str = "";
	//	len = _strlen(str);
//	_puts(str);
	//printf("%s\n", str);
	//rev_string(str);
	//printf("%s\n",str);
//	puts_half(str);
//
	int array[5];

	array[0] = 98;
	array[1] = 402;
	array[2] = -198;
	array[3] = 298;
	array[4] = -1024;
	print_array(array, 5);
	return (0);
}
